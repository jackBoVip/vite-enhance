# Vue DevTools Integration

## 概述

Vue框架插件现在集成了最新版本的 `vite-plugin-vue-devtools`，为Vue 3应用提供强大的开发工具支持。

## 功能特性

### 🔧 **开发工具面板**
- **独立窗口**: 在 `http://localhost:5173/__devtools__/` 访问DevTools界面
- **快捷键**: 按 `Alt+Shift+D` 在应用中切换DevTools
- **实时调试**: 实时查看组件状态、props、事件等

### 🔍 **组件检查器**
- **可视化选择**: 点击页面元素直接定位到组件
- **组件树**: 查看完整的组件层次结构
- **状态检查**: 实时查看组件的data、computed、props等

### 📝 **编辑器集成**
- **一键跳转**: 从DevTools直接跳转到组件源码
- **多编辑器支持**: 支持VS Code、WebStorm、Sublime等主流编辑器

## 配置选项

### 基础配置
```typescript
createVuePlugin({
  devtools: {
    enabled: true,              // 启用/禁用 DevTools
    componentInspector: true,   // 启用组件检查器
    launchEditor: 'code'        // 设置编辑器
  }
})
```

### 完整配置
```typescript
createVuePlugin({
  script: {
    defineModel: true
  },
  devtools: {
    // 是否启用 Vue DevTools (默认: true)
    enabled: true,
    
    // 组件检查器 (默认: true)
    componentInspector: true,
    
    // 编辑器配置 (默认: 'code')
    launchEditor: 'code', // 'code' | 'webstorm' | 'sublime' | 'atom' | 等
    
    // 高级选项: 追加到特定模块而不是body
    appendTo: /main\.ts$/
  }
})
```

## 支持的编辑器

| 编辑器 | 配置值 | 说明 |
|--------|--------|------|
| Visual Studio Code | `'code'` | 默认选择 |
| VS Code Insiders | `'code-insiders'` | 预览版 |
| WebStorm | `'webstorm'` | JetBrains IDE |
| PhpStorm | `'phpstorm'` | PHP IDE |
| PyCharm | `'pycharm'` | Python IDE |
| IntelliJ IDEA | `'idea'` | Java IDE |
| Sublime Text | `'sublime'` | 轻量编辑器 |
| Atom | `'atom'` | GitHub编辑器 |
| Vim | `'vim'` | 终端编辑器 |
| Emacs | `'emacs'` | 终端编辑器 |

## 使用指南

### 1. 启动开发服务器
```bash
pnpm dev
# 或
npm run dev
```

### 2. 访问DevTools
- **方式1**: 打开 `http://localhost:5173/__devtools__/`
- **方式2**: 在应用中按 `Alt+Shift+D`

### 3. 使用组件检查器
1. 在DevTools中启用检查器模式
2. 在页面上点击任意元素
3. 自动定位到对应的Vue组件
4. 查看组件的状态和属性

### 4. 编辑器跳转
1. 在DevTools中选择组件
2. 点击"在编辑器中打开"按钮
3. 自动跳转到组件源码位置

## 开发环境特性

### 🚀 **性能监控**
- 组件渲染时间
- 更新频率统计
- 内存使用情况

### 🎯 **状态管理**
- Vuex/Pinia状态查看
- 状态变更历史
- 时间旅行调试

### 📊 **事件追踪**
- 组件事件监听
- 自定义事件追踪
- 事件传播路径

## 生产环境

Vue DevTools在生产构建时会自动禁用，不会影响生产包大小和性能。

```bash
pnpm build
# DevTools 自动禁用，不包含在生产包中
```

## 故障排除

### 1. DevTools无法访问
**问题**: 无法打开 `/__devtools__/` 页面
**解决**: 
- 确认开发服务器正在运行
- 检查端口是否正确
- 确认 `devtools.enabled` 为 `true`

### 2. 组件检查器不工作
**问题**: 点击页面元素无反应
**解决**:
- 确认 `componentInspector` 为 `true`
- 检查浏览器控制台是否有错误
- 尝试刷新页面

### 3. 编辑器无法打开
**问题**: 点击"在编辑器中打开"无反应
**解决**:
- 确认编辑器已安装并在PATH中
- 检查 `launchEditor` 配置是否正确
- 尝试不同的编辑器配置

### 4. 快捷键不工作
**问题**: `Alt+Shift+D` 无效
**解决**:
- 确认焦点在应用页面上
- 检查是否与其他快捷键冲突
- 尝试直接访问DevTools URL

## 版本信息

- **vite-plugin-vue-devtools**: `^8.0.5`
- **最低Vue版本**: `^3.0.0`
- **最低Vite版本**: `^5.0.0`

## 更多资源

- [Vue DevTools官方文档](https://devtools.vuejs.org/)
- [vite-plugin-vue-devtools GitHub](https://github.com/webfansplz/vite-plugin-vue-devtools)
- [Vue 3官方文档](https://vuejs.org/)

## 示例项目

查看 `examples/vue-app` 目录中的完整示例，了解如何在实际项目中使用Vue DevTools。