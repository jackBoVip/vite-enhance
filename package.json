{
  "name": "vite-enhance-monorepo",
  "version": "1.0.0",
  "description": "A modular Vite enhancement toolkit for enterprise-grade frontend engineering",
  "private": false,
  "type": "module",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm build:main",

    "build:main": "pnpm --filter vite-enhance build",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean",
    "typecheck": "pnpm -r typecheck",
    "version:check": "node scripts/check-versions.js",
    "version:sync": "node scripts/sync-dependencies.js",
    "version:analyze": "node scripts/analyze-workspace.js",
    "version:update": "changeset version",
    "version:release": "pnpm build && changeset publish",
    "changeset": "changeset",
    "changeset:version": "changeset version",
    "changeset:publish": "changeset publish",
    "changeset:status": "changeset status",
    "release": "node scripts/release.js",
    "release:dry-run": "node scripts/release.js --dry-run",
    "replace-catalog-refs": "node scripts/replace-catalog-refs.js",
    "restore-catalog-refs": "node scripts/restore-catalog-refs.js",
    "prepublishOnly": "pnpm replace-catalog-refs && pnpm build"
  },
  "keywords": [
    "vite",
    "frontend",
    "build-tool",
    "monorepo",
    "engineering"
  ],
  "author": "Vite Enhance Kit Team",
  "license": "MIT",
  "devDependencies": {
    "@changesets/cli": "catalog:",
    "@types/node": "catalog:",
    "eslint": "catalog:",
    "glob": "catalog:",
    "js-yaml": "catalog:",
    "prettier": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=9.5.0"
  },
  "packageManager": "pnpm@9.5.0"
}
